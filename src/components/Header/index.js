// @flow
import React from 'react';
import Classes from './Header.module.css';

const Header = () => (
  <header className={Classes.siteTitle}>
    <h1>The Weather App</h1>
  </header>
);

export default Header;
